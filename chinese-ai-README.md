# 通用网页翻译脚本 (AI 增强版) 使用说明

## 📌 功能特性

### v1.0.0 新增功能

1. **白名单管理系统**
   - 默认不翻译任何网站，需要手动添加到白名单
   - 通过脚本菜单轻松管理翻译网站列表

2. **按域名分离缓存**
   - 每个域名独立存储翻译缓存
   - 缓存键格式：`vc_ai_cache_domain_[域名]`
   - 不同网站的翻译缓存互不干扰

3. **便捷的菜单命令**
   - ✅ 将此网站加入翻译名单
   - ❌ 将此网站移出翻译名单
   - 📋 查看翻译名单
   - ⚙️ 翻译设置
   - 📥 导出词典 JSON

## 🚀 使用步骤

### 1. 安装脚本
1. 安装 Tampermonkey 浏览器扩展
2. 安装 `chinese-ai.user.js` 脚本

### 2. 添加网站到翻译名单

**首次访问任何网站时，脚本默认不会翻译**

要启用翻译，请按以下步骤操作：

1. 访问想要翻译的网站
2. 点击浏览器工具栏的 Tampermonkey 图标
3. 选择本脚本的菜单
4. 点击 **"✅ 将此网站加入翻译名单"**
5. 页面会自动刷新并开始翻译

### 3. 配置 AI 翻译

要使用 AI 自动翻译功能：

1. 点击脚本菜单 → **"⚙️ 翻译设置"**
2. 启用 AI 翻译
3. 配置 API 密钥和端点：
   - DeepL API (推荐)
   - OpenAI 兼容 API (GPT, Claude等)

### 4. 管理翻译网站

#### 查看已添加的网站
- 脚本菜单 → **"📋 查看翻译名单"**

#### 移除网站
- 脚本菜单 → **"❌ 将此网站移出翻译名单"**
- 确认后页面会刷新并停止翻译

## 🔧 技术特性

### 缓存机制
- **域名隔离**：每个域名的翻译缓存独立存储
- **自动持久化**：翻译结果自动保存到本地存储
- **缓存有效期**：7天 (可在配置中调整)

### 白名单系统
- **存储位置**：`localStorage` → `vc_translate_whitelist`
- **数据格式**：JSON 数组，存储域名列表
- **示例**：
  ```json
  ["github.com", "stackoverflow.com", "docs.python.org"]
  ```

### 控制台日志

当网站不在白名单中时：
```
[网页翻译] example.com 不在翻译名单中，跳过翻译
[网页翻译] 提示：点击 Tampermonkey 图标 → 本脚本菜单 → "✅ 将此网站加入翻译名单" 来启用翻译
```

当网站在白名单中时：
```
[网页翻译] example.com 在翻译名单中，开始初始化翻译功能
[网页翻译] 初始化完成
- 当前域名: example.com
- 缓存键: vc_ai_cache_domain_example.com
- 缓存: 0 条
- AI翻译: 已启用
- 可见区域翻译: 已启用
```

## 📊 配置项说明

### CONFIG 常量

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `WHITELIST_KEY` | 白名单存储键 | `vc_translate_whitelist` |
| `CACHE_KEY_PREFIX` | 缓存键前缀 | `vc_ai_cache_domain_` |
| `CACHE_TTL_MS` | 缓存有效期 | 7天 |
| `AI_ENABLED_KEY` | AI翻译开关 | `vc_ai_translate_enabled` |

## ⚠️ 注意事项

1. **首次使用**
   - 脚本默认不翻译任何网站
   - 需要手动添加每个网站到白名单

2. **缓存数据**
   - 不同域名的缓存互不影响
   - 移除白名单不会删除缓存数据
   - 如需清除缓存，请手动删除对应的 localStorage 项

3. **性能优化**
   - 只有白名单中的网站才会初始化翻译功能
   - 非白名单网站不会消耗资源

4. **隐私保护**
   - 白名单存储在本地浏览器
   - 不会向外部服务器发送白名单数据

## 🐛 故障排除

### 网站没有翻译
1. 检查是否已添加到白名单：脚本菜单 → "📋 查看翻译名单"
2. 查看控制台日志确认状态
3. 确保 AI 翻译已启用并配置了 API 密钥

### 清除缓存
1. 打开浏览器开发者工具 (F12)
2. Application → Local Storage
3. 找到以 `vc_ai_cache_domain_` 开头的项并删除

### 重置白名单
在控制台执行：
```javascript
localStorage.removeItem('vc_translate_whitelist');
```

## 📝 版本历史

### v1.0.0 (2026-01-15)
- ✅ 实现白名单管理系统
- ✅ 按域名分离缓存
- ✅ 添加脚本菜单命令
- ✅ 移除硬编码的 CORE_TERMS
- ✅ 支持所有网站 (`@match *`)

## 📄 许可证

GPL-3.0 License

## 👤 作者

liyixin21
